# ==============================================================================
# extensions-vscode.txt — Visual Studio Code only
# ==============================================================================
#
# WHAT: Extensions installed into Visual Studio Code but not Cursor.
# WHY : Every id here was checked against Cursor's gallery and is not there.
#       All of them are either Microsoft extensions whose licence restricts
#       them to Microsoft's own products, or small extensions nobody has
#       mirrored. See the registry note in extensions.txt for the query that
#       decides which list an id belongs in.
# HOW : Same format as extensions.txt — one id per line, # for comments.
#
#       The right-hand column below records what Cursor gets instead. Where an
#       equivalent exists it is declared in ../cursor/extensions-cursor.txt, so
#       the two files must be read together: adding a substitute here without
#       adding it there leaves Cursor with a silent gap.
# ==============================================================================

# --- Licence-restricted Microsoft extensions ----------------------------------
# The Python type checker.        Cursor: anysphere.cursorpyright
ms-python.vscode-pylance
# C and C++ IntelliSense.         Cursor: anysphere.cpptools
ms-vscode.cpptools
ms-vscode.cpp-devtools
# Remote development.             Cursor: anysphere.remote-ssh
ms-vscode-remote.remote-ssh
ms-vscode-remote.remote-ssh-edit
ms-vscode.remote-explorer
# Dev containers.                 Cursor: anysphere.remote-containers
ms-vscode-remote.remote-containers

# --- No Cursor equivalent exists ----------------------------------------------
# Markdown preview extras. Not in Cursor's gallery under any publisher, so
# these four features are simply unavailable there. The preview still works —
# it is the checkboxes, emoji shortcodes and footnotes that do not render.
bierner.github-markdown-preview
bierner.markdown-checkbox
bierner.markdown-emoji
bierner.markdown-footnotes
