# ==============================================================================
# extensions.txt — extensions both editors get
# ==============================================================================
#
# WHAT: Extension ids, one per line, installed into both Visual Studio Code and
#       Cursor by install.sh. Blank lines and lines starting with # are ignored.
# WHY : Neither editor has a declarative equivalent of Zed's
#       auto_install_extensions: nothing in settings.json can install an
#       extension, and .vscode/extensions.json holds workspace recommendations
#       only. The installer feeds this list to each editor's CLI.
# HOW : The installer reads the installed set once with `--list-extensions`
#       and runs `code --install-extension <id> --force` (and the same for
#       `cursor`) only for ids not yet installed. `code --list-extensions`
#       regenerates a starting point.
# NOTE: The editors resolve ids against different galleries, so there are
#       three lists: this one, extensions-vscode.txt for VS Code only, and
#       ../cursor/extensions-cursor.txt for Cursor only. README.md § Extensions
#       (in this directory) has the gallery query that decides where an id
#       belongs.
# NOTE: vscodevim.vim is not listed. Both editors are configured as non-modal
#       editors, as Zed is; Neovim is the modal editor here.
# ==============================================================================

# --- Appearance ---------------------------------------------------------------
# The colour theme: the four Catppuccin flavours named in settings.json, and
# the catppuccin.* keys that configure them.
catppuccin.catppuccin-vsc
# File icons, palette-matched to the theme above.
catppuccin.catppuccin-vsc-icons
# Product icons: the toolbar and activity-bar glyphs, which the Catppuccin
# extension does not cover.
miguelsolorio.fluent-icons

# --- Agents -------------------------------------------------------------------
anthropic.claude-code

# --- Python -------------------------------------------------------------------
# The language extension, the debugger, and the environment manager. Cursor's
# gallery serves all three but not Pylance, which anysphere.cursorpyright
# substitutes.
ms-python.python
ms-python.debugpy
ms-python.vscode-python-envs
# The linter and formatter, matching what Neovim runs through its LSP config.
charliermarsh.ruff
# The type checker, matching Neovim's choice.
astral-sh.ty

# --- C, C++ and Swift ---------------------------------------------------------
# The debug adapters. Microsoft's cpptools is in extensions-vscode.txt because
# Cursor's gallery does not carry it.
llvm-vs-code-extensions.lldb-dap
vadimcn.vscode-lldb
ms-vscode.cmake-tools
ms-vscode.makefile-tools
swiftlang.swift-vscode
sweetpad.sweetpad

# --- Other languages ----------------------------------------------------------
rust-lang.rust-analyzer
# Wraps gopls, which comes from the Brewfile. On first use the extension offers
# to `go install` dlv and staticcheck into ~/go/bin, which
# fish/conf.d/00-path.fish puts on PATH; both are in the Brewfile too.
golang.go
# Wraps zls. It and the zig compiler come from the Brewfile, which documents
# the version lock between them.
ziglang.vscode-zig
tamasfe.even-better-toml
redhat.vscode-yaml
bmalehorn.vscode-fish
# Embeds lua-language-server, so no Homebrew formula is needed and the Lua
# entry in ../nvim/.config/nvim/lua/languages.lua stays treesitter-only. The
# settings that make it read Neovim's runtime are in
# .config/Code/User/settings.json.
sumneko.lua
# Embeds lemminx, the Eclipse XML language server, which has no Homebrew
# formula, so the XML entry in ../nvim/.config/nvim/lua/languages.lua is
# treesitter-only.
redhat.vscode-xml

# --- Markdown -----------------------------------------------------------------
yzhang.markdown-all-in-one
davidanson.vscode-markdownlint
bierner.markdown-mermaid
bierner.markdown-preview-github-styles

# --- Containers and CI --------------------------------------------------------
ms-azuretools.vscode-docker
ms-azuretools.vscode-containers
github.vscode-github-actions
