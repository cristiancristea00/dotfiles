# macOS Finder metadata. The .stow-local-ignore files also keep it out of stow.
.DS_Store

# Committed, although they look like state: nvim/.config/nvim/nvim-pack-lock.json
# (pins the plugin revisions a fresh machine installs; see nvim/README.md) and
# git/.config/git/config.work (the work identity; its header says how to make
# it local-only).
