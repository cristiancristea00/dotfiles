# ==============================================================================
# extensions-vscode.txt — Visual Studio Code only
# ==============================================================================
#
# WHAT: Extensions installed into Visual Studio Code but not Cursor.
# WHY : Each id was checked against Cursor's gallery and is not there: either
#       a Microsoft extension whose licence restricts it to Microsoft's own
#       products, or a small extension nobody has mirrored. README.md §
#       Extensions (in this directory) has the gallery query.
# HOW : One id per line; `#` starts a comment. The right-hand column records
#       what Cursor gets instead; each substitute is declared in
#       ../cursor/extensions-cursor.txt, so a change to one file needs a
#       check of the other.
# ==============================================================================

# --- Licence-restricted Microsoft extensions ----------------------------------
# The Python type checker.        Cursor: anysphere.cursorpyright
ms-python.vscode-pylance
# C and C++ IntelliSense.         Cursor: anysphere.cpptools
ms-vscode.cpptools
ms-vscode.cpp-devtools
# Remote development.             Cursor: anysphere.remote-ssh
ms-vscode-remote.remote-ssh
ms-vscode-remote.remote-ssh-edit
ms-vscode.remote-explorer
# Dev containers.                 Cursor: anysphere.remote-containers
ms-vscode-remote.remote-containers

# --- No Cursor equivalent exists ----------------------------------------------
# Markdown preview extras. Not in Cursor's gallery under any publisher, so the
# preview there lacks checkboxes, emoji shortcodes, and footnotes.
bierner.github-markdown-preview
bierner.markdown-checkbox
bierner.markdown-emoji
bierner.markdown-footnotes
